import Menu from '../Menu/MEnu';
import { StyledHeader } from './header.styles';

const Header = () => {
	return (
		<StyledHeader>
			<h1>HEADER</h1>
			<Menu />
		</StyledHeader>
	);
};

export default Header;
